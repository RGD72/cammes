# Story 4.1: Modal de Seleção (ESCOLHER → ESCOLHIDO)

## Status

InReview

## Executor Assignment

executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["lint", "typecheck", "test"]

## Story

**As a** lojista-cliente,
**I want** clicar em "ESCOLHER" em um produto e abrir um modal compacto onde escolho tamanho, cor e quantidade, e ao clicar em "ESCOLHIDO" o item entra no meu carrinho,
**so that** a captura do pedido seja rápida, sem fricção e respeitando as variantes do produto.

## Acceptance Criteria

1. Botão "ESCOLHER" em cada card do grid da vitrine abre modal centralizado responsivo.
2. Modal exibe: imagem grande do produto, referência, descrição, preço formatado em BRL, chips de tamanhos disponíveis (seleção única), chips de cores (seleção única), stepper de quantidade (default 1, min 1, max 99).
3. Se produto não tem múltiplos tamanhos (`sizes.length <= 1`) ou cores (`colors.length <= 1`), omite o respectivo seletor de chips — o campo único é selecionado automaticamente.
4. Botão "ESCOLHIDO" é habilitado apenas quando: se há chips de tamanho → um foi selecionado; se há chips de cor → uma foi selecionada; quantidade ≥ 1.
5. Confirmação chama `addItem` (Server Action de `lib/carts/actions.ts`), fecha modal e exibe toast "Adicionado ao carrinho" (shadcn/ui `toast`). Em caso de erro, exibe toast de erro sem fechar o modal.
6. Modal é totalmente acessível por teclado: focus trap dentro do modal, ESC fecha o modal, stepper utilizável por teclado.
7. Testes unitários: (a) `P-AI-1`: `addItem` com usuário autenticado e input válido passa pela autenticação e validação Zod sem retornar erro de auth/validation (stub retorna `{ ok: false, error.code: 'INTERNAL' }` — a versão com `ok: true` virá quando Story 4.2 implementar o body real); (b) `N-AI-1`: `addItem` retorna `{ ok: false, error.code: 'PERMISSION_DENIED' }` quando usuário não autenticado; (c) `N-AI-2`: `addItem` retorna `{ ok: false, error.code: 'VALIDATION_ERROR' }` quando input inválido (e.g., `quantity: 0`).

## Scope

**IN:**
- `lib/types/index.ts` — adicionar `ApiErrorCode`, `ApiError` e `Result<T>` (tipos planejados em architecture.md §19.2 mas ainda não implementados; bootstrap nesta story pois `addItem` é a primeira action a usá-los)
- `lib/carts/actions.ts` — **NOVO arquivo**: interface `AddItemInput` com schema Zod + `addItem()` Server Action stub; `CartItem` interface local
- `components/brands/select-product-modal.tsx` — novo Client Component (`'use client'`): modal completo com chips de tamanho/cor, stepper, botão "ESCOLHIDO", focus trap nativo, ESC handler
- `components/brands/product-grid-card.tsx` — adicionar botão "ESCOLHER" (hover/focus visible) que chama `onSelect`; `onSelect` já existe como prop opcional
- `app/brands/[slug]/brand-storefront-view.tsx` — adicionar `selectedProduct` state, renderizar `<SelectProductModal>`, fechar após `addItem` bem-sucedido; instalar shadcn/ui Sonner toast (`npx shadcn@latest add sonner`) e adicionar `<Toaster>` ao `app/brands/layout.tsx`
- `tests/unit/carts/add-item-action.test.ts` — 3 casos unitários (P-AI-1, N-AI-1, N-AI-2)

**OUT:**
- Schema de banco (`carts`, `cart_items`) e RLS — Story 4.2 (pré-requisito de implementação)
- `updateQuantity`, `removeItem`, `clearCart`, `getCart` — Story 4.2
- Carrinho tabular `/cart/{slug}` — Story 4.3
- Envio de pedido — Story 4.4
- Badge de contagem de itens no header — Story 4.3
- Animação ou transição do modal — não scopado no MVP

## Dependencies

- Story 3.4 (Done) — `BrandStorefrontView` Client Component + `ProductGridCard` + `getApprovedProductsForStorefront`; o `onSelect` prop já existe em `ProductGridCard` aguardando implementação
- **Story 4.2 — DEVE ser implementada antes desta story para integração completa**: cria tabelas `carts`/`cart_items`, RLS e Server Actions do carrinho (`addItem` incluída). A Story 4.1 define a interface e stub de `addItem`; a implementação real com DB é da Story 4.2.
- `lib/types/index.ts` — `ApiErrorCode`, `ApiError`, `Result<T>` serão adicionados nesta story (bootstrap); `Cart`/`CartItem` completos virão em Story 4.2

## Complexity

**Médio** — 3 pontos

Client Component com gestão de estado local (cor, tamanho, quantidade), focus trap sem biblioteca externa, integração com Server Action. A parte mais delicada é a acessibilidade (focus trap + ESC + ARIA) e a condição de habilitação do botão "ESCOLHIDO". A lógica de negócio do `addItem` é mínima nesta story; o peso vai para Story 4.2.

## Business Value

Fecha o loop de captura de pedido. Sem este modal, o cliente pode ver os produtos na vitrine mas não pode adicionar nada ao carrinho. É o ponto de interação de maior frequência do produto — cada item do pedido passa por aqui.

## Risks

- **Dependência de Story 4.2:** Se 4.2 não existir quando 4.1 for implementada, AC5 não pode ser satisfeito com dados reais. Mitigação: implementar 4.2 antes de 4.1 (recomendado) ou entregar 4.1 com `addItem` retornando mock otimista e wiring real na 4.2.
- **Focus trap sem biblioteca:** Implementar `useFocusTrap` inline (hook simples) evita dependência nova; cobre apenas o caso básico (modal flat, sem nested modals). Risco baixo dado que o modal é simples.
- **Chips de tamanho/cor com dados nulls:** `product.sizes` e `product.colors` são `string[] | null`; o código deve ser defensivo com `?? []`.

## Definition of Done

- [ ] `npm run lint` — 0 erros
- [ ] `npm run typecheck` — 0 erros
- [ ] `npm test` — todos os testes passando (incluindo 3 novos)
- [ ] AC1–AC6 verificados manualmente na vitrine com produto de múltiplos tamanhos/cores
- [ ] AC3 verificado com produto sem variantes (sem seletores)
- [ ] AC6 verificado: modal navegável por teclado, ESC fecha

## Dev Notes

### Previous Story Insights

Story 3.4 estabeleceu o padrão de Server Component (`page.tsx`) + Client Component (`brand-storefront-view.tsx`) para a vitrine. A Story 4.1 expande `brand-storefront-view.tsx` com estado adicional para o modal. **`ProductGridCard` já tem `onSelect?: (product: Product) => void`** — basta adicionar o botão "ESCOLHER" que chama essa prop.

### Data Models

**`Product`** [Source: `lib/products/actions.ts`]:
```typescript
interface Product {
  id: string
  sizes: string[] | null    // chips de tamanho — null ou array vazio = omitir seletor
  colors: string[] | null   // chips de cor — null ou array vazio = omitir seletor
  price_brl: number | null
  image_crop_url: string | null
  reference: string | null
  description: string | null
  look_group: string | null
}
```

**`Cart` / `CartItem`** [Source: docs/architecture.md#4.7]:
```typescript
interface CartItem {
  id: string
  cart_id: string
  product_id: string
  color: string | null
  size: string | null
  quantity: number              // ≥ 1
  unit_price_brl_snapshot: number  // snapshot do price_brl no momento da adição
  total_brl: number             // quantity * unit_price_brl_snapshot
  added_at: string
}
```

### API Specification

**`addItem()`** — nova Server Action em `lib/carts/actions.ts` (stub nesta story; DB real em Story 4.2):

```typescript
'use server'

import { z } from 'zod'
import { createServerSupabaseClient } from '@/lib/supabase/server'
import type { Result } from '@/lib/types/index'

export interface CartItem {
  id: string
  cart_id: string
  product_id: string
  color: string | null
  size: string | null
  quantity: number
  unit_price_brl_snapshot: number
  total_brl: number
  added_at: string
}

const AddItemSchema = z.object({
  brandId: z.string().uuid(),
  productId: z.string().uuid(),
  color: z.string().nullable(),
  size: z.string().nullable(),
  quantity: z.number().int().min(1).max(99),
  unitPriceBrl: z.number().min(0),
})

export type AddItemInput = z.infer<typeof AddItemSchema>

export async function addItem(input: AddItemInput): Promise<Result<CartItem>> {
  const supabase = await createServerSupabaseClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) {
    return { ok: false, error: { code: 'PERMISSION_DENIED', message: 'Não autenticado', timestamp: new Date().toISOString(), requestId: crypto.randomUUID() } }
  }
  const parsed = AddItemSchema.safeParse(input)
  if (!parsed.success) {
    return { ok: false, error: { code: 'VALIDATION_ERROR', message: 'Dados inválidos', timestamp: new Date().toISOString(), requestId: crypto.randomUUID() } }
  }
  // NOTE: implementação real em Story 4.2 (tabelas carts + cart_items)
  // Stub: retorna erro para forçar implementação da Story 4.2 antes de usar em produção
  return { ok: false, error: { code: 'INTERNAL', message: 'Carrinho não implementado — Story 4.2 necessária', timestamp: new Date().toISOString(), requestId: crypto.randomUUID() } }
}
```

> [Source: docs/architecture.md#5.1 — `lib/actions/carts.ts: addItem`]

**Nota sobre o stub:** O stub retorna erro explícito. Isso garante que os testes unitários validem o caminho de autenticação e validação Zod. A implementação real (upsert em `carts`, insert em `cart_items`) fica em Story 4.2, que substitui o stub pelo body real.

### Component Specification

**`components/brands/select-product-modal.tsx`** — Client Component (`'use client'`):

Props:
```typescript
interface Props {
  product: Product | null   // null = modal fechado
  brandId: string
  onClose: () => void
  onAdded: () => void       // callback após addItem bem-sucedido
}
```

Estrutura do modal:
- Overlay backdrop com `onClick` → `onClose`
- Dialog `role="dialog"` `aria-modal="true"` `aria-labelledby`
- Coluna esquerda: imagem grande (ou fallback SVG)
- Coluna direita: referência, descrição, preço em BRL
- Seção chips de TAMANHO (se `product.sizes?.length > 1`)
- Seção chips de COR (se `product.colors?.length > 1`)
- Stepper de quantidade: `-` button / input / `+` button (min=1, max=99)
- Botão "ESCOLHIDO" (disabled quando campos obrigatórios incompletos)
- Focus trap: on mount, focar primeiro elemento focável; Tab/Shift+Tab cicla dentro do modal; ESC → `onClose`

Focus trap approach (sem biblioteca):
```typescript
// useFocusTrap hook inline no arquivo do modal
function useFocusTrap(ref: React.RefObject<HTMLElement>, active: boolean) {
  useEffect(() => {
    if (!active || !ref.current) return
    const focusable = ref.current.querySelectorAll<HTMLElement>(
      'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
    )
    const first = focusable[0]
    const last = focusable[focusable.length - 1]
    first?.focus()
    function handleTab(e: KeyboardEvent) {
      if (e.key !== 'Tab') return
      if (e.shiftKey) { if (document.activeElement === first) { e.preventDefault(); last?.focus() } }
      else { if (document.activeElement === last) { e.preventDefault(); first?.focus() } }
    }
    function handleEsc(e: KeyboardEvent) { if (e.key === 'Escape') onClose() }
    document.addEventListener('keydown', handleTab)
    document.addEventListener('keydown', handleEsc)
    return () => { document.removeEventListener('keydown', handleTab); document.removeEventListener('keydown', handleEsc) }
  }, [active])
}
```

**`components/brands/product-grid-card.tsx`** — modificação mínima:
- Adicionar botão "ESCOLHER" com `onClick={() => onSelect?.(product)}`
- Botão visível em hover/focus: `opacity-0 group-hover:opacity-100 focus-within:opacity-100`
- O card article já tem `className="group"` ou adicionar

**`app/brands/[slug]/brand-storefront-view.tsx`** — modificação:
```typescript
const [selectedProduct, setSelectedProduct] = useState<Product | null>(null)
// ...
<SelectProductModal
  product={selectedProduct}
  brandId={brand.id}
  onClose={() => setSelectedProduct(null)}
  onAdded={() => { setSelectedProduct(null); toast({ title: 'Adicionado ao carrinho' }) }}
/>
```

### File Locations

[Source: docs/architecture.md#2.3 + convenções do projeto]
- `lib/carts/actions.ts` — novo arquivo (domínio carrinho)
- `components/brands/select-product-modal.tsx` — novo componente
- `components/brands/product-grid-card.tsx` — modificação (adicionar botão)
- `app/brands/[slug]/brand-storefront-view.tsx` — modificação (adicionar modal state)
- `tests/unit/carts/add-item-action.test.ts` — 3 casos

### Testing Requirements

[Source: padrão de Story 3.4 — `tests/unit/brands/storefront-actions.test.ts`]

```typescript
// tests/unit/carts/add-item-action.test.ts
// P-AI-1: addItem retorna { ok: true, data: CartItem } quando usuário autenticado e insert ok
// N-AI-1: addItem retorna { ok: false } quando usuário não autenticado
// N-AI-2: addItem retorna { ok: false } quando Supabase retorna erro

vi.mock('@/lib/supabase/server', () => ({
  createServerSupabaseClient: vi.fn(),
}))
```

**Estratégia de P-AI-1 (com stub):** Verificar que, com usuário autenticado e input válido, o retorno NÃO é `PERMISSION_DENIED` nem `VALIDATION_ERROR` — ou seja, os guards de autenticação e validação foram superados. O stub retorna `INTERNAL`, que é o comportamento esperado nesta story. Em Story 4.2, P-AI-1 será ajustado para verificar `{ ok: true, data: CartItem }` com o mock do insert.

### Technical Constraints

- **`'use client'` obrigatório** para `SelectProductModal` (state, event handlers, focus trap)
- **`brand-storefront-view.tsx` já é Client Component** — pode importar `SelectProductModal` diretamente
- **shadcn/ui `toast`**: verificar se `useToast` + `Toaster` estão instalados (`components/ui/toast.tsx`); se não, instalar via `npx shadcn-ui@latest add toast`
- **Focus trap sem `@radix-ui/react-dialog`**: o modal é construído manualmente (acesso a `role="dialog"` + foco) para evitar re-instalar Dialog do shadcn; se shadcn Dialog já estiver disponível, usar em vez do custom trap
- **`product.sizes` e `product.colors` são `string[] | null`**: defensivamente usar `product.sizes ?? []`
- **Stepper:** inputs numéricos devem ter `type="number"` com `min="1" max="99"` para acessibilidade; controles +/- são botões (não inputs)

## Tasks / Subtasks

- [x] **Task 1 — Bootstrap tipos globais + Server Action `addItem` stub** (AC: 5, 7)
  - Adicionar a `lib/types/index.ts`: `ApiErrorCode` (union de strings), `ApiError` (interface), `Result<T>` (union ok/error) — fonte: `docs/architecture.md §19.2`
  - Criar `lib/carts/actions.ts` com `CartItem` interface, `AddItemInput` Zod schema e `addItem()` stub
  - Autenticação via `createServerSupabaseClient().auth.getUser()` — retorna `{ ok: false, error: { code: 'PERMISSION_DENIED' } }` se não autenticado
  - Validação Zod do input — retorna `{ ok: false, error: { code: 'VALIDATION_ERROR' } }` se inválido
  - Stub: retorna `{ ok: false, error: { code: 'INTERNAL', message: 'Carrinho não implementado — Story 4.2 necessária' } }`
  - Output: `Result<CartItem>` usando o `Result<T>` adicionado ao `lib/types/index.ts`

- [x] **Task 2 — Testes unitários** (AC: 7)
  - Criar `tests/unit/carts/add-item-action.test.ts`
  - `P-AI-1`: mock de usuário autenticado + input válido → verifica que `error.code` NÃO é `'PERMISSION_DENIED'` nem `'VALIDATION_ERROR'` (auth e validação passaram; stub retorna `'INTERNAL'` — aceitável pois o real virá na Story 4.2)
  - `N-AI-1`: mock de `auth.getUser()` retornando `{ data: { user: null } }` → verifica `{ ok: false, error.code: 'PERMISSION_DENIED' }`
  - `N-AI-2`: mock de usuário autenticado + `quantity: 0` (inválido) → verifica `{ ok: false, error.code: 'VALIDATION_ERROR' }`
  - Executar `npm test` — todos passando

- [x] **Task 3 — Botão "ESCOLHER" no ProductGridCard** (AC: 1)
  - Modificar `components/brands/product-grid-card.tsx`
  - Adicionar `className="group"` ao article (ou verificar se já existe)
  - Adicionar botão "ESCOLHER" no rodapé do card com hover/focus visibility
  - Chamar `onSelect?.(product)` no click

- [x] **Task 4 — Modal `SelectProductModal`** (AC: 2, 3, 4, 6)
  - Criar `components/brands/select-product-modal.tsx` como Client Component
  - Overlay + dialog com `role="dialog" aria-modal="true"`
  - Imagem do produto (ou fallback SVG)
  - Exibir referência, descrição, preço formatado
  - Seção chips de TAMANHO (renderizar só se `sizes.length > 1`)
  - Seção chips de COR (renderizar só se `colors.length > 1`)
  - Auto-select quando array tem exatamente 1 elemento
  - Stepper de quantidade (min 1, max 99, default 1)
  - Botão "ESCOLHIDO" disabled até campos obrigatórios preenchidos (AC4)
  - `useFocusTrap` hook inline (focus trap + ESC)

- [x] **Task 5 — Integração na BrandStorefrontView + Toast** (AC: 1, 5)
  - Instalar shadcn/ui Sonner: `npx shadcn@latest add sonner`
  - Adicionar `<Toaster />` ao `app/brands/layout.tsx` (uma vez, persiste para toda a área de cliente)
  - Modificar `app/brands/[slug]/brand-storefront-view.tsx`
  - Adicionar `useState<Product | null>(null)` para `selectedProduct`
  - Passar `onSelect={setSelectedProduct}` ao `ProductGridCard`
  - Renderizar `<SelectProductModal>` condicionalmente
  - No `onAdded`: fechar modal + chamar `toast('Adicionado ao carrinho')` via Sonner
  - Em caso de erro da action: chamar `toast.error('Erro ao adicionar — tente novamente')` sem fechar o modal

- [x] **Task 6 — Quality gates** (DoD)
  - `npm run lint` → 0 erros
  - `npm run typecheck` → 0 erros
  - `npm test` → todos passando (incluindo 3 novos)

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation will use manual review process only.

## File List

**Criados:**
- `lib/carts/actions.ts` — `CartItem` interface, `AddItemInput` Zod schema, `addItem()` Server Action stub
- `components/brands/select-product-modal.tsx` — modal Client Component (ESCOLHER/ESCOLHIDO)
- `tests/unit/carts/add-item-action.test.ts` — 3 casos (P-AI-1, N-AI-1, N-AI-2)

**Modificados:**
- `lib/types/index.ts` — adicionados `ApiErrorCode`, `ApiError`, `Result<T>`
- `components/brands/product-grid-card.tsx` — adicionar botão "ESCOLHER"
- `app/brands/[slug]/brand-storefront-view.tsx` — adicionar modal state + `<SelectProductModal>` + toast Sonner
- `app/brands/layout.tsx` — adicionar `<Toaster />` do Sonner

## Dev Agent Record

**Agent Model Used:** claude-sonnet-4-6

**Completion Notes:** Implementação completa. `addItem` stub em `lib/carts/actions.ts` retorna `INTERNAL` (implementação real em Story 4.2). Modal construído sem biblioteca externa — `useFocusTrap` hook inline. Sonner instalado (v2.0.7) para toasts. Focus trap, ESC, ARIA e chips de cor/tamanho implementados. 96/96 testes passando.

## QA Results

—

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-05-23 | @sm (River) | Story 4.1 criada (Draft) a partir do PRD Epic 4 Story 4.1 |
| 2026-05-23 | @po (Pax) | Validação GO (8/10) — corrigidos Result<T>, toast e P-AI-1; Status: Draft → Ready |
| 2026-05-23 | @dev (Dex) | Implementação completa — 6/6 tasks; Status: Ready → InProgress → InReview |
