# Story 1.3: Autenticação Admin e Cliente (Login/Logout)

## Status

InReview

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run lint", "npm run typecheck", "npm run test", "manual smoke: login admin + cliente, logout, redirect 403"]
```

## Story

**As a** usuário (admin ou cliente),
**I want** fazer login com e-mail e senha, ser redirecionado conforme meu papel e poder sair com segurança,
**so that** eu acesse apenas a área que me corresponde e minha sessão seja protegida.

## Acceptance Criteria

1. Página `/login` exibe formulário com campos de e-mail e senha, validações inline (campo obrigatório, formato de e-mail) e mensagens de erro claras em caso de credenciais inválidas.
2. Após login bem-sucedido, o sistema lê o `role` em `users_profile` e redireciona: `admin → /admin`, `customer → /brands`.
3. Sessões usam cookies `HttpOnly`, `Secure`, `SameSite=Lax` (configurados via `@supabase/ssr`).
4. Botão de logout presente em qualquer área autenticada encerra a sessão no Supabase Auth e redireciona para `/login`.
5. Tentativa de acesso a `/admin/*` por usuário `customer` (ou vice-versa para `/brands/*`) é redirecionada para a área correta pelo middleware de Next.js.
6. Falhas de login acumuladas >5 por minuto no mesmo IP retornam 429 (Too Many Requests) com mensagem clara.

## Scope

### IN

- Supabase SSR client helpers: `lib/supabase/server.ts`, `lib/supabase/client.ts`, `lib/supabase/middleware.ts`
- TypeScript auth types em `lib/types/index.ts`: `UserProfile`, `AuthSession`
- Server Actions de autenticação em `lib/auth/actions.ts`: `loginAction`, `logoutAction`
- Página de login: `app/(auth)/login/page.tsx` + `app/(auth)/layout.tsx`
- `middleware.ts` na raiz do projeto — proteção de rotas `/admin/*` e `/brands/*` + refresh de sessão
- Rate limiting de login no Server Action (>5 falhas/min/IP → 429) usando tabela Supabase ou contador em memória via Edge
- Componente `LogoutButton` em `components/ui/logout-button.tsx` (Server Component com form action)
- Placeholders vazios de `/admin` e `/brands` para validar redirecionamento (conteúdo real vem na Story 1.5)
- Testes unitários para `loginAction` (credenciais inválidas, role redirect) e middleware (rota protegida sem sessão)

### OUT

- UI/layout visual completo de `/admin` e `/brands` (Story 1.5)
- Magic link e recuperação de senha (Story 1.4)
- Formulário de cadastro de novos usuários (manual via Supabase Studio no MVP)
- Integração com `audit_logs` para eventos de login/logout (Story 5.5)
- Rate limiting persistente em banco ou Redis (MVP: in-memory ou lógica básica no action)
- Notificações de login suspeito / múltiplos dispositivos (pós-MVP)

## Dependencies

- **Story 1.1:** Done — Next.js 15 + `@supabase/ssr ^0.5.2` + `@supabase/supabase-js ^2.45.4` instalados; `.env.local` com `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- **Story 1.2:** Done — tabela `users_profile` com coluna `role user_role` (`admin|customer`) existe no schema; RLS habilitada
- **Pré-requisito:** Supabase local (`supabase start`) ou Supabase Cloud com URL/keys configuradas para testar login

## Complexity Estimate

**T-shirt size: M** (estimativa: 3–5 horas)

Razão: múltiplos arquivos de infra de auth (server/client/middleware helpers), Server Actions, middleware.ts, página de login com form + validação, e testes. Padrão `@supabase/ssr` é bem documentado e determinístico — risco baixo, mas coordenação entre camadas aumenta o esforço.

## Business Value

Habilita o acesso seguro e diferenciado por papel — sem esta story, nenhum fluxo de admin ou cliente pode ser testado end-to-end. Pré-requisito direto para as Stories 1.4 (magic link), 1.5 (dashboards funcionais) e todos os Épicos 2–5.

## Risks

- `SEC-001` (herança da Story 1.2): a policy RLS `users_profile_admin_read_customers` é auto-referencial. Se não corrigida antes desta story, a leitura do `role` pós-login pode falhar para admins. Mitigação: usar função `SECURITY DEFINER is_admin()` como indicado no QA Gate 1.2, ou ler o role via `auth.jwt()` claims em vez de query direta.
- Supabase local pode não ter usuários de teste cadastrados — criar manualmente 1 admin e 1 customer via Supabase Studio antes de testar.
- Rate limiting in-memory não persiste entre instâncias Vercel em produção. MVP acceptable; anotar como tech debt para substituição por Vercel KV ou Upstash antes do go-live.

## Definition of Done

- `npm run lint && npm run typecheck && npm run test` passam sem regressão
- Login com admin válido redireciona para `/admin`; login com customer válido redireciona para `/brands`
- Credenciais inválidas exibem mensagem de erro na página de login (sem reveal de informação sensível)
- Acessar `/admin` sem sessão redireciona para `/login` (verificado manualmente)
- Acessar `/admin` como customer redireciona para `/brands` (verificado manualmente)
- Logout encerra sessão e redireciona para `/login`
- Story 1.4 (magic link) pode ser iniciada sem bloqueio de dependência

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

---

## Tasks / Subtasks

- [x] **Task 1 — Supabase SSR client helpers** (AC: 3)
  - [x] 1.1 Criar `lib/supabase/server.ts` — helper `createServerClient()` usando `@supabase/ssr` e `cookies()` do Next.js para uso em Server Components e Server Actions
  - [x] 1.2 Criar `lib/supabase/client.ts` — helper `createBrowserClient()` usando `@supabase/ssr` para uso em Client Components
  - [x] 1.3 Criar `lib/supabase/middleware.ts` — helper `updateSession()` que refresh o token de sessão em cada requisição (obrigatório para manter sessão ativa com `@supabase/ssr`)
  - [x] 1.4 Garantir que `.env.local` tem `NEXT_PUBLIC_SUPABASE_URL` e `NEXT_PUBLIC_SUPABASE_ANON_KEY` (documentados em `.env.local.example`)

- [x] **Task 2 — TypeScript auth types** (AC: 2, 3)
  - [x] 2.1 Em `lib/types/index.ts`, adicionar:
    - `UserProfile` interface (id, role: `'admin' | 'customer'`, full_name, email, phone, is_active, created_at, updated_at)
    - `UserRole` type alias (`'admin' | 'customer'`)
    - `AuthSession` interface (user_id, role, email) para uso no middleware e Server Components
  - [x] 2.2 Re-exportar tipos de `lib/types/index.ts` (barrel export)

- [x] **Task 3 — Server Actions: login e logout** (AC: 1, 2, 4, 6)
  - [x] 3.1 Criar `lib/auth/actions.ts` com `'use server'`
  - [x] 3.2 Implementar `loginAction(formData: FormData)`:
    - Extrair `email` e `password` do `formData`
    - Chamar `supabase.auth.signInWithPassword({ email, password })`
    - Em caso de erro: retornar `{ error: 'Credenciais inválidas. Verifique e-mail e senha.' }` (sem revelar qual campo está errado)
    - Em caso de sucesso: consultar `users_profile` para obter `role`; redirecionar via `redirect('/admin')` ou `redirect('/brands')` conforme `role`
    - Rate limiting: verificar contador de falhas por IP — se >5 no último minuto, retornar `{ error: 'Muitas tentativas. Aguarde 1 minuto e tente novamente.', status: 429 }`
  - [x] 3.3 Implementar `logoutAction()`:
    - Chamar `supabase.auth.signOut()`
    - Redirecionar via `redirect('/login')`

- [x] **Task 4 — Middleware de proteção de rotas** (AC: 3, 5)
  - [x] 4.1 Criar `middleware.ts` na raiz do projeto (ao lado de `package.json`)
  - [x] 4.2 Chamar `updateSession()` em todas as rotas para refresh do token
  - [x] 4.3 Lógica de proteção:
    - Se rota começa com `/admin`: verificar sessão e `role === 'admin'`; se não autenticado → redirect `/login`; se autenticado mas `role === 'customer'` → redirect `/brands`
    - Se rota começa com `/brands` ou `/cart` ou `/orders`: verificar sessão e `role === 'customer'`; se não autenticado → redirect `/login`; se autenticado mas `role === 'admin'` → redirect `/admin`
    - Rota `/login`: se já autenticado, redirecionar para área correta conforme `role`
  - [x] 4.4 Configurar `config.matcher` para excluir `_next/static`, `_next/image`, `favicon.ico` e arquivos estáticos

- [x] **Task 5 — Página de login** (AC: 1)
  - [x] 5.1 Criar `app/(auth)/layout.tsx` — layout simples centralizado para área pública (sem sidebar)
  - [x] 5.2 Criar `app/(auth)/login/page.tsx` com:
    - `<form>` com action apontando para `loginAction`
    - Campo `email` (type="email", required, autocomplete="email")
    - Campo `password` (type="password", required, autocomplete="current-password")
    - Botão submit "Entrar" com estado de loading (use `useFormStatus` em Client Component wrapper)
    - Exibição de `error` retornado pela action (texto vermelho abaixo do formulário)
    - Exibição de status 429 com mensagem específica de rate limit
    - Acessível: labels explícitos associados via `htmlFor`, foco visível, aria-describedby para erros (NFR23)
  - [x] 5.3 Usar componentes `shadcn/ui`: `Input`, `Button`, `Label`, `Alert` (ou equivalentes já disponíveis)

- [x] **Task 6 — Placeholders de /admin e /brands** (AC: 2, 5)
  - [x] 6.1 Criar `app/admin/page.tsx` — placeholder mínimo: `<h1>Área Admin</h1>` (conteúdo real vem na Story 1.5)
  - [x] 6.2 Criar `app/admin/layout.tsx` — layout vazio que inclui `LogoutButton`
  - [x] 6.3 Criar `app/brands/page.tsx` — placeholder mínimo: `<h1>Área do Cliente</h1>` (conteúdo real vem na Story 1.5)
  - [x] 6.4 Criar `app/brands/layout.tsx` — layout vazio que inclui `LogoutButton`

- [x] **Task 7 — Componente LogoutButton** (AC: 4)
  - [x] 7.1 Criar `components/ui/logout-button.tsx`:
    - Server Component com `<form>` action apontando para `logoutAction`
    - Botão "Sair" com ícone opcional (Lucide `LogOut`)
    - Acessível: aria-label="Encerrar sessão"

- [x] **Task 8 — Rate limiting básico** (AC: 6)
  - [x] 8.1 Implementar contador in-memory em `lib/auth/rate-limit.ts`:
    - `Map<string, { count: number; windowStart: number }>` indexado por IP
    - `checkRateLimit(ip: string): boolean` — retorna `true` se bloqueado (>5 falhas no último minuto)
    - `recordFailure(ip: string): void` — incrementa contador
    - `resetOnSuccess(ip: string): void` — limpa contador após login bem-sucedido
  - [x] 8.2 Integrar `checkRateLimit` em `loginAction` antes de chamar `signInWithPassword`
  - [x] 8.3 Adicionar comentário de tech debt: "Rate limiter in-memory — substituir por Vercel KV ou Upstash antes de produção"

- [x] **Task 9 — Testes** (AC: todos)
  - [x] 9.1 `tests/unit/auth/login-action.test.ts`:
    - Teste: credenciais inválidas retornam `{ error: 'Credenciais inválidas...' }`
    - Teste: rate limit ativo retorna `{ error: 'Muitas tentativas...', status: 429 }` após 5 falhas
    - Teste: login admin bem-sucedido resolve role `admin`
    - Teste: login customer bem-sucedido resolve role `customer`
    - Mock: `@supabase/ssr` e `supabase.auth.signInWithPassword`
  - [x] 9.2 `tests/unit/auth/rate-limit.test.ts`:
    - Teste: < 5 tentativas não bloqueia
    - Teste: exatamente 6 tentativas no mesmo IP no mesmo minuto bloqueia
    - Teste: contador reseta após 1 minuto

- [x] **Task 10 — Validação de regressão** (AC: todos)
  - [x] 10.1 Executar `npm run lint && npm run typecheck && npm run test` — todos devem passar
  - [ ] 10.2 Smoke test manual (requer Supabase local ou cloud com usuários de teste):
    - Acessar `/login` sem sessão → página de login exibida
    - Login com admin válido → redireciona para `/admin`
    - Login com customer válido → redireciona para `/brands`
    - Acessar `/admin` como customer → redireciona para `/brands`
    - Clicar em logout → redireciona para `/login`
    - Acessar `/admin` sem sessão → redireciona para `/login`

---

## Dev Notes

### Contexto Arquitetural

**Fonte canônica:** `docs/prd/4-technical-assumptions.md`, `docs/prd/2-requirements.md` (FR1–FR4, NFR7–NFR10, NFR23), `docs/architecture/data-model-skeleton.md`

### Insight da Story 1.2 (SEC-001)

A policy RLS `users_profile_admin_read_customers` é auto-referencial — pode falhar ao ler o `role` de um admin. Alternativas para `loginAction`:

**Opção A (recomendada):** Ler o `role` via `auth.jwt()` claims se o campo estiver no JWT claim customizado. Supabase permite adicionar metadata ao JWT via Hook de Auth. Consultar se há hook configurado.

**Opção B (fallback seguro):** Usar client com `service_role` key em Server Action (nunca exposta ao browser) para query sem RLS. Criar helper separado `createServiceClient()` usando `SUPABASE_SERVICE_ROLE_KEY` (variável privada, não prefixada com `NEXT_PUBLIC_`).

**Opção C:** Corrigir a policy antes desta story (criar função `SECURITY DEFINER is_admin()` como sugerido no QA Gate 1.2).

> @dev: avaliar qual opção é mais segura/simples para o MVP. Documentar decisão no Change Log.

[Source: docs/stories/1.2.story.md#QA Results → SEC-001]

### Supabase SSR: Padrão Correto para Next.js 15 App Router

```typescript
// lib/supabase/server.ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createServerSupabaseClient() {
  const cookieStore = await cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return cookieStore.getAll() },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch { /* Server Component — ignorar */ }
        },
      },
    }
  )
}
```

> [Source: @supabase/ssr docs — Next.js App Router pattern; `@supabase/ssr ^0.5.x`]

### Supabase SSR: Middleware updateSession

```typescript
// lib/supabase/middleware.ts
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function updateSession(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return request.cookies.getAll() },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value))
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )
  // CRÍTICO: não remover este getUser — atualiza o cookie de sessão
  const { data: { user } } = await supabase.auth.getUser()
  return { supabaseResponse, user, supabase }
}
```

> [Source: @supabase/ssr docs — Next.js Middleware pattern]

### Cookies de Sessão

`@supabase/ssr` configura automaticamente os cookies com `HttpOnly`, `Secure`, `SameSite=Lax` quando chamado no lado do servidor. Não é necessário configurar manualmente os atributos dos cookies — o package gerencia isso.

> [Source: docs/prd/2-requirements.md#NFR9; @supabase/ssr ^0.5.x]

### Rate Limiting — MVP Approach

```typescript
// lib/auth/rate-limit.ts
const attempts = new Map<string, { count: number; windowStart: number }>()

const WINDOW_MS = 60_000  // 1 minuto
const MAX_ATTEMPTS = 5

export function checkRateLimit(ip: string): boolean {
  const now = Date.now()
  const entry = attempts.get(ip)
  if (!entry || now - entry.windowStart > WINDOW_MS) return false
  return entry.count >= MAX_ATTEMPTS
}

export function recordFailure(ip: string): void {
  const now = Date.now()
  const entry = attempts.get(ip)
  if (!entry || now - entry.windowStart > WINDOW_MS) {
    attempts.set(ip, { count: 1, windowStart: now })
  } else {
    entry.count++
  }
}

export function resetOnSuccess(ip: string): void {
  attempts.delete(ip)
}
```

**TECH DEBT:** Implementação in-memory — não persiste entre instâncias Vercel. Substituir por Vercel KV ou Upstash antes de go-live em produção.

> [Source: docs/prd/2-requirements.md#NFR10]

### Obter IP no Server Action

```typescript
// No loginAction, obter IP via headers:
import { headers } from 'next/headers'

const headersList = await headers()
const ip = headersList.get('x-forwarded-for')?.split(',')[0]?.trim()
  ?? headersList.get('x-real-ip')
  ?? '127.0.0.1'
```

### Estrutura de Arquivos

```
app/
├── (auth)/
│   ├── layout.tsx                    # Layout público (centrado)
│   └── login/
│       └── page.tsx                  # Página de login
├── admin/
│   ├── layout.tsx                    # Layout admin (com LogoutButton)
│   └── page.tsx                      # Placeholder admin
└── brands/
    ├── layout.tsx                    # Layout cliente (com LogoutButton)
    └── page.tsx                      # Placeholder cliente

components/
└── ui/
    └── logout-button.tsx             # Componente de logout

lib/
├── supabase/
│   ├── server.ts                     # createServerSupabaseClient()
│   ├── client.ts                     # createBrowserClient()
│   └── middleware.ts                 # updateSession()
├── auth/
│   ├── actions.ts                    # loginAction, logoutAction
│   └── rate-limit.ts                 # checkRateLimit, recordFailure, resetOnSuccess
└── types/
    └── index.ts                      # UserProfile, UserRole, AuthSession

middleware.ts                         # Raiz do projeto — proteção de rotas

tests/
└── unit/
    └── auth/
        ├── login-action.test.ts
        └── rate-limit.test.ts
```

> [Source: docs/prd/4-technical-assumptions.md#4.1-repository-structure]

### TypeScript Types

```typescript
// lib/types/index.ts (expandido nesta story)
export type UserRole = 'admin' | 'customer'

export interface UserProfile {
  id: string
  role: UserRole
  full_name: string
  email: string
  phone: string | null
  is_active: boolean
  created_at: string
  updated_at: string
}

export interface AuthSession {
  user_id: string
  role: UserRole
  email: string
}
```

> [Source: docs/architecture/data-model-skeleton.md#users_profile]

### Testing

**Framework:** Vitest 2.1 (já configurado em Story 1.1)

**Estratégia de mock para `loginAction`:** Mockar `@supabase/ssr` e `next/headers` via `vi.mock()`. O padrão de Server Actions (uso de `next/headers` e `redirect`) requer mock cuidadoso — usar `vi.mock('next/headers', () => ({ cookies: vi.fn() }))` e `vi.mock('next/navigation', () => ({ redirect: vi.fn() }))`.

**Localização dos testes:** `tests/unit/auth/`

> [Source: docs/prd/4-technical-assumptions.md#4.3-testing-requirements]

### Shadcn/ui Componentes

Componentes disponíveis (instalados em Story 1.1): `Button`, `Input`, `Label`. Se `Alert` não estiver disponível, usar `<div role="alert">` com Tailwind classes para mensagens de erro.

Verificar: `ls components/ui/` antes de criar novos componentes para evitar duplicação.

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### File List

| File | Action |
|------|--------|
| `lib/supabase/server.ts` | created |
| `lib/supabase/client.ts` | created |
| `lib/supabase/middleware.ts` | created |
| `lib/types/index.ts` | modified |
| `lib/auth/rate-limit.ts` | created |
| `lib/auth/actions.ts` | created |
| `middleware.ts` | created |
| `app/(auth)/layout.tsx` | created |
| `app/(auth)/login/page.tsx` | created |
| `app/(auth)/login/_components/login-form.tsx` | created |
| `app/admin/page.tsx` | created |
| `app/admin/layout.tsx` | created |
| `app/brands/page.tsx` | created |
| `app/brands/layout.tsx` | created |
| `components/ui/logout-button.tsx` | created |
| `tests/unit/auth/rate-limit.test.ts` | created |
| `tests/unit/auth/login-action.test.ts` | created |

### Completion Notes

- SEC-001 analisado: `users_profile_self_read` (`id = auth.uid()`) cobre leitura do próprio role sem recursão. Client normal é suficiente para o login flow — não foi necessário service role.
- `useActionState` (React 19) usado no LoginForm em vez de `useFormState` (depreciado).
- CodeRabbit desabilitado — revisão manual pelo @qa.
- Smoke test manual (Task 10.2) requer Supabase local com usuários de teste — não executado no ambiente de CI.

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-05-21 | 1.0 | Story criada para Epic 1 — Foundation, Auth & Multitenancy Skeleton | River (@sm) |
| 2026-05-21 | 1.1 | Validated GO (9/10) — Status: Draft → Ready | Pax (@po) |
| 2026-05-21 | 1.2 | Development started (YOLO mode) — Status: Ready → InProgress | @dev |
| 2026-05-21 | 1.3 | Development complete — Status: InProgress → InReview | @dev |
